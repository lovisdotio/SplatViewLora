import { VFXBatch } from './VFXBatch';
import { ParticleSystem } from './ParticleSystem';
import { Object3D } from 'three';
export declare class BatchedRenderer extends Object3D {
    batches: Array<VFXBatch>;
    systemToBatchIndex: Map<ParticleSystem, number>;
    type: string;
    constructor();
    private static equals;
    addSystem(system: ParticleSystem): void;
    deleteSystem(system: ParticleSystem): void;
    updateSystem(system: ParticleSystem): void;
    update(delta: number): void;
}
