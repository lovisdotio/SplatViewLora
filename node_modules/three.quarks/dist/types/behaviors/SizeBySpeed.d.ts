import { Behavior } from './Behavior';
import { Particle } from '../Particle';
import { FunctionValueGenerator, IntervalValue } from '../functions';
export declare class SizeBySpeed implements Behavior {
    size: FunctionValueGenerator;
    speedRange: IntervalValue;
    type: string;
    initialize(particle: Particle): void;
    constructor(size: FunctionValueGenerator, speedRange: IntervalValue);
    update(particle: Particle): void;
    toJSON(): any;
    static fromJSON(json: any): Behavior;
    frameUpdate(delta: number): void;
    clone(): Behavior;
    reset(): void;
}
